package code.snippet

import scala.xml.Unparsed

object contactComment {
  def render = Unparsed("""<!-- Smart people always read the fine print, and they always check the source code.  Let's chat, email me at byron at the same domain. -->""")
}
